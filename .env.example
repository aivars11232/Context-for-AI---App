# Optional local bootstrap values. Copy to .env only when needed; do not commit it.
# Model, storage, validation, and logging values belong in validated local YAML.
CONTEXT_FOR_AI_ENV=development
# CONTEXT_FOR_AI_CONFIG_DIR=./config
