MVP status: `docs/contracts/ConfigurationAndLogging.md` is canonical for the
six YAML files, schema, defaults, precedence, validation, and loopback-only
Ollama policy. The historical example below must not override it.

config/
├── app.yaml
├── models.yaml
├── context.yaml
├── validation.yaml
├── memory.yaml
└── logging.yaml


Historical example only. MVP uses `maximum_prompt_tokens`,
`reserved_response_tokens`, `minimum_relevance_score: 0.35`, and
`validation.max_revisions: 0..2` as specified by the canonical contract.
