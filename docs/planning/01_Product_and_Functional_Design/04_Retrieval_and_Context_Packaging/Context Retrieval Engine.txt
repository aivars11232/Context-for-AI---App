This searches memory and selects only what is relevant.

MVP status: Arch Dock is a historical fixture. MVP uses only the deterministic
keyword/project/topic/recency/importance/scope scoring rules in
`docs/contracts/DomainAndDecisionRules.md`.
For a request about Arch Dock, it might retrieve:
- Arch Dock project state
- active visual style
- previous panel description
- current output constraints
- recent user corrections

It should not retrieve unrelated information.
The retrieval engine needs for MVP:
keyword search
project filtering
recency weighting
relevance scoring
correction priority
duplicate removal

Semantic search is Deferred Post-MVP and is not an MVP dependency.
A simple scoring system could be:
Context relevance =
topic match
+ project match
+ reference match
+ recency
+ instruction priority
+ correction importance
