ProcessUserMessage
    ↓
Persist exact user message and idempotent processing run
    ↓
Load versioned conversation state, entities, and eligible memories
    ↓
Interpret user message deterministically
    ↓
Resolve references
    ↓
Extract constraints
    ↓
Retrieve relevant memory
    ↓
Build context packet
    ↓
Persist deterministic decisions and immutable packet
    ↓
Start and await buffered local model request outside SQLite transaction
    ↓
Persist and validate complete candidate; correct at most twice if necessary
    ↓
Persist linked assistant message and state only on valid result
    ↓
Otherwise persist clarification/controlled failure; never mutate memory automatically
    ↓
Return final text, clarification, or safe failure to UI
